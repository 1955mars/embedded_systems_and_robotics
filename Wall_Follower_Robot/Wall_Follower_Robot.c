#include<avr/io.h>
#include<util/delay.h>
#include<string.h>
#include<mars/lcd.h>
main()
{
	DDRB=0xFF;
	DDRA=0xFC;
	while(1)
	{
		if(PINA==3)
		{
			PORTB=4;
			_delay_ms(10);
		}
		if(PINA==2)
		{
			PORTB=5;
			_delay_ms(10);
		}
		if(PINA==0)
		{
			PORTB=1;
			_delay_ms(10);
		}

	}
}
